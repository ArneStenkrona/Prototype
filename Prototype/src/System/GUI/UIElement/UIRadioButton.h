#pragma once
#include "UIButtonBase.h"
/*
class RadioButtonGroup {
public:
private:
};

class UIRadioButton : public UIButtonBase {
public:
    //Creates an instance of UIButton with listener _listener.
    //The listener is destroyed when UIButton is destroyed
    UIRadioButton(UIActionListener* _listener, int _positionX, int _positionY, int _width, int _height,
        int _layer, std::string _text);
    ~UIRadioButton();

protected:

private:
    void onMouseOver();
    void update();
    void render();

    //mouse is over
    bool mouseOver;
    //User has initiated click on button
    bool holdClick;

    UIActionListener* listener;
};*/